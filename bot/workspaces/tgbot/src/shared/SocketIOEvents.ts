export const SOCKET_IO_EVENTS = (assembledEvent: string) => ({
	_user_update: `${assembledEvent}_user_update`,
	_ready2fight: `${assembledEvent}_ready2fight`,
})

// = (assembledEvent: string) => ({
// 	_user_update: `${assembledEvent}_user_update`,

// })
