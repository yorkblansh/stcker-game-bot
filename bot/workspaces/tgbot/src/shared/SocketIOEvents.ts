export const SocketIOEvents = {
	_user_update: (assembledEvent: string) => `${assembledEvent}_user_update`,
}

// = (assembledEvent: string) => ({
// 	_user_update: `${assembledEvent}_user_update`,

// })
